# Instructions for Codex

This is a self-contained Daisy Maison content concept. Load
`CURRENT_CONTEXT.md`, `CREATIVE_BRIEF.md`, `PRODUCTION_PLAN.md` and the exact
master image before reviewing or implementing anything.

When invoked from Claude, act as an independent read-only supervisor unless Max
explicitly grants implementation authority. Challenge product drift, weak match
geometry, unnecessary AI generation, unsafe assumptions and scope expansion.
Do not call Claude back during the same round.

If Max starts directly in Codex and explicitly requests Claude collaboration,
use the real Claude CLI from the Daisy Maison workspace according to the root
`AGENTS.md` and `COLLABORATION.md`. Never simulate the counterpart.

Accepted decisions must be written back to `CURRENT_CONTEXT.md`. Preserve the
immutable master photograph and put all derivatives in new directories.

